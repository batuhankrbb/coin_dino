import 'package:flutter/material.dart';

extension HelperExtensions on BuildContext {
/// Global kenar yuvarlama fonksiyonu.
 BorderRadius borderRadiusAll(double radius) =>
      BorderRadius.all(Radius.circular(radius));
}
